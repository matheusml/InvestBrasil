<!DOCTYPE html>
<html>
<head>
  <title>InvestBrasil</title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application", "companies" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/">InvestBrasil</a>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to "Empresas", companies_path %></li>
          </ul>

          <%= form_tag(companies_path, :class => 'navbar-search pull-left') do %>
            <%= text_field_tag :company, '', :class => "search-query span2", :placeholder => 'Procurar empresas'%>
           <% end %> 

          <div class="btn-group pull-right">
            <% if current_user %>
              <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
                <i class="icon-user"></i> <%= current_user.name %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Editar", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to "Sair", signout_path, id: "sign_out" %></li>
              </ul>
            <% else %>
                <b><%= link_to "Login", log_in_path, :class => 'btn' %></b>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <div class="span9"><%= yield %></div>
    </div>
  </div>
</body>
</html>  
